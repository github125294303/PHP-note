类与对象：对象是由类的实例得到的，定义类使用class关键字，后面跟着类名，在定义类时，不需要给类传参数，
	类主要分为两部分：1.类属性
			 2.类方法
		定义类属性与类方法都要设置可见性（访问控制）。
		魔术方法：构造函数（实例类时自动执行） __construct。
	实例类需要使用new关键字，得到对象。
	对象中使用类属性$obj->name。
	对象中使用类方法$obj->name()。 （区别在于右括号的是方法）
（返回原型对象的构造函数）

预定义变量：